<%= form_for @number, url: {action: "create"} do |f| %>
  <%= label_tag(:value, "Please input your number. Should be a positive integer") %>
  <%= f.text_field :value %>
  <%= f.submit "Submit" %>
<% end %>

